export function DemoBadge() {
  return (
    <span
      className="pill border border-amber-400/30 bg-amber-400/10 text-amber-200"
      title="Shape profiles are real (cached from shapes.inc). Conversation metrics are deterministic simulated fixtures."
    >
      <span className="h-1.5 w-1.5 rounded-full bg-amber-300" />
      Demo data
    </span>
  );
}
